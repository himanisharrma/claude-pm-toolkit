---
description: Analyze a product-data file (funnel / cohort / segmentation)
argument-hint: <path to CSV/JSON> [+ the question]
---
Use the **data-analyst** subagent on: **$ARGUMENTS**
Inspect the schema, compute the relevant view (funnel drop-off, cohort retention, or segmentation), show the
numbers, and report the headline finding + biggest opportunity + data caveats + recommended next step.
